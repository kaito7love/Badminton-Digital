VITE_API_BASE_URL=http://localhost:5000/api/v1

# Hiện khối "Tài khoản thử nghiệm" (mật khẩu demo) trên trang đăng nhập của bản build.
# Chỉ bật cho bản demo công khai — khi đó tài khoản admin vẫn không hiện.
# `npm run dev` luôn hiện đủ, kể cả admin.
VITE_SHOW_DEMO_ACCOUNTS=false
